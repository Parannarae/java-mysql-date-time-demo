spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver # the newer driver compare to `com.mysql.jdbc.Driver`
        url: jdbc:mysql://127.0.0.1:3306/test?connectionTimeZone=LOCAL&forceConnectionTimeZoneToSession=true
        username: root
        password: somePassword
